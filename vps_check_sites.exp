#!/usr/bin/expect -f

set timeout 30
set h "157.10.252.74"
set u "sipanda"
set p "Sipanda123#"

spawn ssh -o StrictHostKeyChecking=no $u@$h
expect "password:"
send "$p\r"
expect "*\\$ "
send "ls -la /etc/nginx/sites-available/\r"
expect "*\\$ "
send "exit\r"
expect eof
