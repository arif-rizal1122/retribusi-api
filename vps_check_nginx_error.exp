#!/usr/bin/expect -f
set timeout 10
spawn ssh sipanda@157.10.252.74 -t "sudo tail -n 20 /var/log/nginx/error.log"
expect "password:"
send "Sipanda123#\r"

expect {
    "password for sipanda:" {
        send "Sipanda123#\r"
        exp_continue
    }
    eof
}
