#!/usr/bin/expect -f
set timeout 60
set password "[REDACTED]"
set host "157.10.252.74"
set user "sipanda"

spawn ssh $user@$host "sudo cat /etc/nginx/sites-available/retribusi-frontend-prod"
expect "password:"
send "$password\r"
expect {
    "password:" { send "$password\r"; exp_continue }
    eof
}
