#!/usr/bin/expect -f
set timeout 30
set password "[REDACTED]"
set host "157.10.252.74"
set user "sipanda"

spawn ssh $user@$host "cat /home/sipanda/retribusi-api/.env"
expect "password:"
send "$password\r"
expect {
    "password:" { send "$password\r"; exp_continue }
    eof
}
